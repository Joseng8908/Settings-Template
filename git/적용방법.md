1. .gitmessage.txt 파일을 홈 디렉토리(~/.gitmessage.txt)에 만들고 위의 템플릿을 저장하세요.

2. 터미널에서 다음 명령어를 입력합니다:
git config --global commit.template ~/.gitmessage.txt

3. 이제 git commit을 입력하면 기본적으로 해당 템플릿이 에디터에 나타납니다.

(4. git config --global core.editor "vim" -> vim을 사용해서 커밋 메시지 작성)